# Pegify Distribution

This repository is the distribution surface for the Pegify desktop app. It
contains only end-user install metadata and release workflows.

## Install

```bash
curl -fsSL https://pegify.dev/install.sh | bash
```

Manual download is available from the releases page. The installer detects your
platform and architecture, verifies the downloaded binary, and starts the local
daemon without requiring a source clone.
